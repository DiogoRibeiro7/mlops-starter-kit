name: CI

on:
  push:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Set up Python
        uses: actions/setup-python@v7
        with:
          python-version: '3.10'
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install poetry
          poetry install
      - name: Lint
        run: |
          poetry run flake8 src/ tests/
      - name: Test
        run: |
          poetry run pytest --cov=src --cov-report=xml --maxfail=1 --disable-warnings -q
      - name: Upload coverage
        uses: actions/upload-artifact@v4
        with:
          name: coverage-xml
          path: coverage.xml
      - name: Integration Tests
        run: |
          poetry run pytest tests/integration --maxfail=1 --disable-warnings -q
      - name: Smoke test container
        run: |
          docker build -f docker/serve.Dockerfile -t mlops-starter-kit:serve .
          docker run -d -p 8000:8000 --name api mlops-starter-kit:serve
          trap 'docker logs api; docker rm -f api' EXIT
          curl --fail --retry 15 --retry-all-errors --retry-delay 2 --retry-max-time 60 --max-time 5 http://localhost:8000/
