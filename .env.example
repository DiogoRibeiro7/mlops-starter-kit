# Optional serving configuration. Start the API after training this version.
MODEL_PATH=artifacts/models/baseline_model/v1.pkl

# Legacy environment-based helpers; new jobs use confs/*.yaml.
PROJECT_NAME=mlops-starter
DATASET_PATH=data/raw
DATASET_FILENAME=example.csv
TARGET_COL=target
TEST_SIZE=0.2
RANDOM_SEED=42
